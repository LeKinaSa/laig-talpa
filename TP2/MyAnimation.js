/**
 * MyAnimation class: represents an animation
**/
class MyAnimation {
    /**
     * @constructor
     */
    constructor(scene, update) {
        this.scene        = scene;
        this.updatePeriod = update;
    }
}

    